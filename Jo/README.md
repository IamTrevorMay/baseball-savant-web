# Jo's Brain — Index

> Read `JO.md` for the persona and `context/triton-context.md` for the platform, pipelines, and
> operator this brain serves. This index is the map: **33 reference docs across 3 domains**, plus
> **3 applied playbooks** that translate each domain into specific Triton recommendations.
> Each doc has a TL;DR up top, inline evidence grades (measured/documented/inferred/folklore), and
> cited sources at the bottom.
>
> **Jo owns: "is the data there, fresh, and complete."** Not "is the number defensible" (→ `Li`)
> or "is it presented honestly" (→ `Cas`) or "is the metric a good idea" (→ `Soto`).

**Build status:** foundation complete (persona, context, index). Reference docs in progress —
see per-domain status markers below. `[ ]` = planned, `[x]` = built and researched.

## Context
- [triton-context.md](context/triton-context.md) — Ground truth on the Triton data platform: the
  8s RPC statement-timeout ceiling, the 17 crons, real table/index sizes, the 2026 Stuff+ outage
  post-mortem, and the backfill route that never worked. **Read before any applied advice.**

## Applied Playbooks (start here for "what should we do?")
- [ ] [data-reliability-applied.md](applied/data-reliability-applied.md) — Now/Next/Later for
  Triton: wiring `reportError` to a real sink, coverage monitors on derived columns, auditing the
  other 16 crons for the 8s cap, making `trackCronRun` prove work happened.
- [ ] [postgres-performance-applied.md](applied/postgres-performance-applied.md) — The chunking
  playbook for `pitches` (29 indexes, 4.8GB), dead-tuple/bloat management, index audit, whether
  `pitches`/`retro_events` should be partitioned, and the `run_mutation_long` question.
- [ ] [data-quality-applied.md](applied/data-quality-applied.md) — Expectation suite for `pitches`
  and derived columns, constraint hardening, MiLB casing normalization, cross-source reconciliation
  against the MLB Stats API, and safe-backfill patterns.

## Domain 1: Data Reliability Engineering (`data-reliability/`)
- [x] [01-pipeline-observability-fundamentals.md](data-reliability/01-pipeline-observability-fundamentals.md) — The "job ran" vs "job did the work" distinction; why partial degradation is worse than total failure (the Stuff+ coverage table where row counts stayed perfect and averages moved 0.8); the five pillars plus **Coverage** as a sixth for derived columns; golden signals and where the Errors analogy breaks; the four instrumentation tiers (run-metadata table → assertions → baselines → lineage); Write-Audit-Publish and why only its audit step fits Triton; industry TTD/TTR benchmarks vs Triton's 90-day TTD. 24 sources.
- [ ] [02-data-freshness-slos.md](data-reliability/02-data-freshness-slos.md) — Freshness/latency SLIs and SLOs for data products, error budgets, staleness thresholds, how to pick them without alert fatigue.
- [ ] [03-volume-completeness-monitoring.md](data-reliability/03-volume-completeness-monitoring.md) — Row-count anomaly detection, coverage ratios on derived columns, gap/hole detection in time series, seasonality-aware baselines.
- [ ] [04-alerting-oncall-design.md](data-reliability/04-alerting-oncall-design.md) — Alert design, dead-man switches and heartbeats, paging vs ticketing, symptom vs cause alerts, alert fatigue economics for a one-person team.
- [ ] [05-orchestration-scheduling.md](data-reliability/05-orchestration-scheduling.md) — Cron vs Airflow/Dagster/Temporal, idempotency, retries and backoff, DAG gating, backfill semantics, what you give up with plain cron.
- [ ] [06-failure-modes-taxonomy.md](data-reliability/06-failure-modes-taxonomy.md) — The catalogue: silent partial writes, swallowed exceptions, upstream schema drift, late data, retry storms, clock skew, poison records. With detection strategy per mode.
- [ ] [07-incident-response-forensics.md](data-reliability/07-incident-response-forensics.md) — Diagnosing data incidents, distinguishing "data is wrong" from "the world changed," root-cause discipline, blameless post-mortems, correction/communication protocol.
- [ ] [08-lineage-impact-analysis.md](data-reliability/08-lineage-impact-analysis.md) — Column- and table-level lineage, dependency graphs, blast-radius analysis before a change, tooling landscape.
- [ ] [09-external-api-ingestion.md](data-reliability/09-external-api-ingestion.md) — Third-party ingestion reliability (MLB Stats API, Baseball Savant): rate limits, retries/backoff, pagination, contract drift, restatement, caching, defensive parsing.
- [ ] [10-observability-tooling.md](data-reliability/10-observability-tooling.md) — Monte Carlo, Bigeye, Elementary, Soda, OpenTelemetry, Sentry, Grafana; self-hosted vs SaaS; realistic costs and what a solo operator should actually buy.
- [ ] [11-serverless-cron-reliability.md](data-reliability/11-serverless-cron-reliability.md) — Vercel/serverless execution limits, `maxDuration`, cold starts, at-least-once vs at-most-once cron delivery, idempotency under retry, long-job decomposition.

## Domain 2: Postgres & Analytical DB Performance (`postgres-performance/`)
- [ ] [01-query-planning-explain.md](postgres-performance/01-query-planning-explain.md) — Reading `EXPLAIN (ANALYZE, BUFFERS)`, plan node types, cost model, statistics and `ANALYZE`, why the planner picks a seq scan, plan regressions.
- [ ] [02-indexing-strategy.md](postgres-performance/02-indexing-strategy.md) — btree/partial/covering/expression/GIN/BRIN, multicolumn ordering, index-only scans, write amplification, unused-index detection, the cost of 29 indexes.
- [ ] [03-timeouts-locks-concurrency.md](postgres-performance/03-timeouts-locks-concurrency.md) — `statement_timeout`/`lock_timeout`/`idle_in_transaction_session_timeout`, role config and `SET ROLE` inheritance semantics, lock levels, deadlocks, `pg_locks`.
- [ ] [04-bulk-write-patterns.md](postgres-performance/04-bulk-write-patterns.md) — Chunking strategy, `COPY`, batched UPDATE/upsert, `ctid` vs keyset paging (and why OFFSET paging is unsafe), HOT updates, fillfactor, write amplification math.
- [ ] [05-vacuum-autovacuum-bloat.md](postgres-performance/05-vacuum-autovacuum-bloat.md) — MVCC, dead tuples, autovacuum tuning, freeze/wraparound, bloat measurement and remediation, `VACUUM FULL` vs `pg_repack`.
- [x] [06-partitioning-large-tables.md](postgres-performance/06-partitioning-large-tables.md) — RANGE/LIST/HASH; plan-time vs run-time pruning (`Subplans Removed` vs `(never executed)`) and why prepared statements defeat the former; partition-wise join/aggregate and the `work_mem`×partitions memory trap; the unique-must-include-partition-key rule and why it would **weaken** Triton's 13.9M-scan upsert guarantee into a silent-duplicate risk; ATTACH/DETACH lock levels, `DETACH CONCURRENTLY`, the default-partition scan trap; `pg_partman`; three live-migration strategies (ATTACH / view+trigger / build-beside) and the two things that actually block them here — ~2× transient disk and the 8s RPC cap. **Verdict: don't partition anything.** `pitches` is already index-pruned (`Index Cond: ((game_year = 2026) AND (pitch_type = 'FF'))`, no seq scan), `retro_events` has *no time column at all*, and the one real win — bounded per-partition VACUUM — is beaten by dropping the **1,417 MB of `pitches` indexes with `idx_scan = 0`** and running the vacuum that has fired **once, ever** (`autovacuum_count = 1`, 16.2% dead tuples, `Heap Fetches: 914`). 20 sources.
- [x] [07-postgrest-supabase-architecture.md](postgres-performance/07-postgrest-supabase-architecture.md) — **Why every RPC call is capped at 8s**, proven both directions: PostgREST logs in as `authenticator` (8s) and `SET ROLE` does not re-apply `rolconfig`, so `service_role`'s NULL config inherits nothing. The PostgREST request lifecycle including its real generated `pgrst_source` SQL; JWT claims → `request.jwt.claims`; Triton's three `SECURITY DEFINER` RPCs and an honest tradeoff audit (no RLS, no plan cache, no `db-max-rows`, no `EXPLAIN`); the measured `jsonb_agg` tax (849 ms for 5k×90 cols, >120s at 10k — it took the API offline); `run_mutation`'s double-execution; Supavisor/PgBouncer transaction-mode breakage; RLS `initPlan` cost and Triton's 258/170/**0** policy census; `safeupdate`; Micro-class compute limits. 25 sources.
- [x] [08-aggregation-materialization.md](postgres-performance/08-aggregation-materialization.md) — Store-vs-compute and why the *grain* you materialize at matters more than whether you do (`stuff_plus` per-pitch vs `pitcher_season_command` per season vs `wRC+` computed in JS); plain `REFRESH` vs `CONCURRENTLY` (unique-index requirement, why CONCURRENTLY is usually *slower*, the nullable-unique-index landmine at 1700ms→9000ms, DELETE/INSERT bloat); rollup tables and incremental `ON CONFLICT` maintenance; staleness windows, `pg_cron` scheduling, and catalog vs *data* ordering dependencies; five ways to detect a stale matview and why `relfilenode` is a dead signal under CONCURRENTLY. **Contains a live incident: `refresh_materialized_views()`, `refresh_league_averages()` and `refresh_league_percentiles()` have `proconfig = NULL`, so all three have timed out at the 8s cap on 50 of 52 nights since 2026-06-21 while `cron_runs` recorded `success` every time — `league_averages` is 46 days stale.** 22 sources.
- [x] [09-schema-design-analytical.md](postgres-performance/09-schema-design-analytical.md) — Rows-per-page as the driver of every analytical cost; measured column-ordering win (5,770 → 4,616 pages, −20%) and `float8` → `real` win (4,928 → 2,880 pages, −41.6%); `numeric` as a 2.4× CPU tax (`pitches.stuff_plus`); the 23-byte tuple header and `ceil(natts/8)` null bitmap (121 columns → `t_hoff` 40); why `DROP COLUMN` reclaims nothing; TOAST at the ~2 kB tuple threshold and why AES-256-GCM Whoop `raw_data` defeats pglz compression; JSONB 61% larger than `text`; STORED-only generated columns on PG 17.6; the anatomy of `pitches` at 575.5 B/row vs a ~440 B ideal — where the gap is 13.9% dead tuples, not padding. **Anti-recommendation: never rewrite a live table for alignment alone.** 23 sources.
- [x] [10-monitoring-postgres.md](postgres-performance/10-monitoring-postgres.md) — The query cookbook for a live Postgres. `pg_stat_statements` setup/columns and the mean-vs-total trap; **the measured proof that timed-out statements are never recorded**, which makes `max_exec_time` a censored distribution and therefore a *leading* saturation indicator; the four Triton workloads pinned at 94.8–100.0% of their timeout ceiling; `pg_stat_user_tables` dead tuples, the per-table autovacuum trigger arithmetic, and HOT-ratio as index write amplification (4.0% on `pitches` vs 96.1% on `sos_scores`); the 9 zero-scan indexes on `pitches` (~1.48 GB, ~1.02 GB droppable) and why `idx_scan = 0` isn't a drop signal for constraint-backing indexes; `pg_stat_activity`, wait event types, `auto_explain` (loaded but with a threshold *above* the 8s cap, so structurally inert); why the 38.5% cache hit ratio is the wrong thing to act on; `pg_stat_io`, bloat estimation, lock/connection monitoring; alert-vs-dashboard triage. 20 sources.
- [x] [11-capacity-storage-planning.md](postgres-performance/11-capacity-storage-planning.md) — **The repo's "8GB disk plan" guidance is stale by ~4×: the database measured 32.3 GiB and five tables are 99.2% of it.** Measured per-row cost (`pitches` 575.5 B heap + 570.8 B index); growth modelling (~810k rows/season → 0.91 GiB/yr, ~1.8 GiB/yr platform-wide, ~5.6%); index storage as half of all growth with ~1.9 GB droppable; bloat as recoverable space (1.17 GB); what actually happens when a volume fills (WAL PANIC, why `VACUUM` can't save you, why `VACUUM FULL` on `pitches`/`retro_events` would fail on disk); Supabase's 90%-auto-expand / 95%-read-only guardrails and the disk-never-shrinks rule; the cost curve (disk $0.125/GB vs the compute step function behind a 38.51% cache hit ratio); archival ranking with `retro_events` (20.84 GB, 12 lifetime seq scans) as the single biggest win; and why TOAST/compression settings save Triton exactly zero bytes. 23 sources.

## Domain 3: Data Quality Engineering (`data-quality/`)
- [x] [01-data-quality-dimensions.md](data-quality/01-data-quality-dimensions.md) — The framing doc for the domain. Why DAMA UK's six, DMBOK2's eight and ISO/IEC 25012's fifteen disagree and why it barely matters; the four-part test (expression / window / threshold / action) a dimension must pass to be real; ISO 8000-8's syntactic/semantic/pragmatic split as the cut that actually tells you which fix applies. A master table mapping all 11 dimensions → SQL measurement → Triton instance → sibling doc, then per-dimension depth: **completeness measured per derived column** (the Stuff+ table where row counts stayed perfect), **accuracy** as the only non-introspective dimension, **consistency** — including the live, newly-found `milb_pitches.events` regression (100% Title Case 2023–25, 53.5/46.5 split in 2026, caused by commit `410212b` shipping a normalization without a backfill), **timeliness** as schedule- not clock-anchored, **validity** (`stuff_plus` clamped in three files, zero `CHECK` constraints on `pitches`), **uniqueness** as the one dimension enforced structurally. Anti-recommendation against composite quality scores. 22 sources.
- [x] [02-declarative-expectations.md](data-quality/02-declarative-expectations.md) — Expectation suites as code compared on the axis that decides it: **what runtime must be running**. GX Core 1.x, dbt tests (+dbt-utils, +the now-unmaintained dbt-expectations), Soda Core/SodaCL, Pandera, Deequ/PyDeequ, Elementary; known vs unknown unknowns and why build-time tests don't run continuously. The Triton finding: **`lib/dataIntegrity.ts` is already an expectation suite that has produced `fail` zero times in 95 run days / 776 results**, and two of its eight checks return `pass` on query error (`:108`, `:167`). Measured assertion cost with `EXPLAIN` — 7-day coverage 9.9s cold vs 0.53s warm, 7-day uniqueness 18.3s vs 16ms at 2 days — proving assertions here are *not* millisecond-cheap and must be date-scoped. Ships a concrete TypeScript `{name, sql, predicate, severity}` suite, a three-state severity model (`pass`/`breached`/`could-not-evaluate`), and a `data_quality_metrics` rollup. Anti-recommendation: adopt none of the tools. 23 sources.
- [x] [03-constraint-design-postgres.md](data-quality/03-constraint-design-postgres.md) — Constraints as the cheapest validation layer, and why Triton under-uses them: `stuff_plus` is clamped in application SQL (`route.ts:322`) with no `CHECK`, and `integrity_checks.status` documents its legal values in a comment. The catalogue with add-time lock and write cost per type; enum vs lookup table vs `CHECK`; domains and the `NOT NULL`-in-a-domain trap; deferrable constraints **and why a `DEFERRABLE` key would break every `ON CONFLICT` upsert in the repo**. The two-step for a live 8.89M-row table: `NOT VALID` + `VALIDATE CONSTRAINT` (`SHARE UPDATE EXCLUSIVE`, doesn't block writers) and `CREATE UNIQUE INDEX CONCURRENTLY` + `ADD CONSTRAINT … USING INDEX`. The `pitches.pitcher → players.id` FK analysed in full: write cost is negligible, but `syncNewPlayers` runs *after* the pitch upsert, so the FK would silently and permanently drop every debut player's pitches — plus the finding that `checkOrphanedPitchers` returns `status: 'pass'` on its own query error. 18 sources.
- [x] [04-distribution-drift-detection.md](data-quality/04-distribution-drift-detection.md) — Shewhart/CUSUM/EWMA on data metrics (~44 vs ~10 subgroups for a 1σ shift) and why a trailing baseline absorbed the 3%/week Stuff+ decay; PSI's formula, credit-risk bands and their large-sample critique, with **measured Triton PSI of 0.0020 (2025→2026) and 0.0082 (2022→2023) across the sweeper's rise** — versus **χ² = 720, p < 10⁻¹⁰⁰** on the same buckets; JS/Wasserstein/Hellinger/L∞ and why Wasserstein suits the physics columns; categorical drift and new-category detection (`ST`/`SV` present back to 2015, `FT` resurrected in 2026 with 13 rows, `checkNewPitchNames` warning 54 times and never failing); reference windows, seasonality and multiple testing across 90+ columns; **the decision procedure for real-change vs artifact** (8 discriminators + an 8-step protocol, worked on Povich-as-real and Stuff+-as-artifact); the blind spot that **drift on non-null values cannot see a completeness failure**; and computing all of it off a histogram snapshot table under the 8s cap. 19 sources.
- [x] [05-anomaly-detection-timeseries.md](data-quality/05-anomaly-detection-timeseries.md) — **Why anomaly detection would NOT have caught the Stuff+ outage, with the arithmetic:** a 7-day rolling mean + 25% rule misses the April→May decay by ~20× and would need a **157-day window** to fire, while a fixed `coverage >= 0.95` floor fires ~93 days earlier. Point/contextual/collective anomalies; spike vs level shift vs drift and the `s·(w+1)/2` bound that makes rolling baselines structurally blind to ramps; mean/σ self-masking (`(n−1)/√n` max z-score) vs median/MAD; STL and seasonal-naive baselines; why Prophet/ARIMA *absorb* the failure; how much history each method needs; false-positive economics grounded in `integrity_checks` (776 results, 0 fails ever, `materialized_views` warned on 96.6% of days) and the 2-day normal Statcast lag. **Recommends building this last.** 22 sources.
- [x] [06-reconciliation-source-of-truth.md](data-quality/06-reconciliation-source-of-truth.md) — Cross-source agreement checks vs. the *system of record* declaration, and why you need both plus a code-resident exclusion list. Triton's seven-source map and the finding that **`player_season_stats` has no `strikeouts`/`base_on_balls` columns**, so the K/BB check against the MLB Stats API is impossible until two INTs are added — Retrosheet (`event_type` = Chadwick `EVENT_CD`) covers it meanwhile. Grain strategy (detect at season, localise at game, never start at row) and a five-class tolerance table: exact / exact+exclusions / epsilon / relative+floor / statistical. Runnable SQL for the game-count check and the season K/BB/BF reconciliation, both on `FULL OUTER JOIN`. Traps: the **base-3 `inningsPitched`** (`62.1` = 62⅓), the **MiLB Title Case** silent zero, and Statcast restating history. A declared system-of-record table (official totals → `player_season_stats`; PA events → `retro_events`; measurement → `pitches`) plus a six-cause triage order in which "we lost rows" is *last*. Extends `integrity_checks` with no DDL. Anti-recommendation: no row-level pitch-to-play diffing, no data-diff tool. 18 sources.
- [x] [07-null-semantics-missingness.md](data-quality/07-null-semantics-missingness.md) — The five meanings SQL collapses into one NULL (unknown / not-applicable / not-yet-collected / era-not-covered / zero-written-blank), and the three-valued-logic surprises with runnable SQL: `NOT IN` over a NULL-bearing subquery returning zero rows, inequality dropping NULLs (which Triton's `pitch_type NOT IN ('PO','IN')` SP/RP rule does today), `UNIQUE` permitting unlimited duplicate NULL keys, `CHECK` passing on NULL, `ORDER BY DESC` leading with NULLs. **Centerpiece: `AVG()` skipping NULLs is the mechanism of the 2026 Stuff+ outage** — coverage 99.5%→0% while the displayed monthly league average moved ~0.8, and the argument that *normalized plus-stats camouflage missingness better than raw columns* because any subset re-centres on 100. Shows that `stuff_plus_n = COUNT(p.stuff_plus)` was already materialized nightly beside `avg_stuff_plus` (`scripts/create-materialized-views.sql:105,323,374`) and never read. Names the concrete defect: `pitches.stuff_plus` is NULL for three indistinguishable reasons (not-applicable / no-baseline / pipeline-failure) and proposes a partial-index fix instead of a table-rewriting generated column. MCAR/MAR/MNAR with the case that Triton's missingness is MNAR (tracking failures correlate with parks and conditions) — an accuracy problem, not just coverage. Anti-recommendation: no `NOT NULL` on `stuff_plus`, no backfilled defaults, no sentinels. 17 sources.
- [x] [08-duplicate-detection-idempotency.md](data-quality/08-duplicate-detection-idempotency.md) — Natural vs surrogate keys; `ON CONFLICT` semantics and the four traps (exact-set arbiter inference and the `42P10` it raises, `excluded`, NULLs-distinct on nullable keys, in-batch `21000 cardinality_violation`); why MERGE is wrong here; idempotency keys (Stripe/Brandur) vs content idempotency, and the rule against retrying relative writes. **The Triton finding: the ingest is correctly idempotent — unique index + 3-day overlap is the retry layer Vercel doesn't provide — but `app/api/update/route.ts:148` is an unconditional `ON CONFLICT DO UPDATE` that rewrites ~8k unchanged rows a night**, now running at **~94.8% of the 8s cap**; the `WHERE ... IS DISTINCT FROM` fix needs raw SQL via `run_mutation` and also repairs `totalInserted`, which counts no-op updates and leaves `skipDownstream` permanently dead. Uniqueness inventory with *(verify)* markers, and detection queries scoped to 2–3 days (2-day scan 16.4 ms vs 7-day 18,302 ms). Anti-recommendation: not `ignoreDuplicates: true`. 17 sources.
- [x] [09-schema-evolution-contracts.md](data-quality/09-schema-evolution-contracts.md) — What a contract actually specifies (and why implementations fail organizationally, not technically); Confluent's BACKWARD/FORWARD/FULL compatibility taxonomy and how it maps onto a registry-less CSV ingest. The Triton finding: **zero contract enforcement at the Savant boundary** — and the drift asymmetry is backwards from the standard advice, since an *added* column is forwarded to PostgREST and fails ~12,000 requests while a *removed* one silently becomes NULL. The headline: **coverage monitoring cannot catch the next Stuff+ outage**, because the scoring UPDATE guards only `release_speed IS NOT NULL` and `COALESCE`s the other three terms to zero (`route.ts:319-333`) — losing `pfx_x`/`pfx_z`/`release_extension` leaves `stuff_plus` 100% populated and quietly velocity-only, with `refreshPitchBaselines` leaving baselines *stale rather than absent*. Ships the cheap fix: a `game_pk` header test replacing `csv.length < 100`, a column-set hash with project-and-alert, and batch-level `cov`/`oob` assertions on the four inputs. Plus `ingest_quarantine` (the per-row retry loop already isolates bad rows and discards them), restatement as a contract problem via the `pitch_name` → `pitch_baselines` join, and expand-migrate-contract as the *only* mechanically possible schema change on `pitches`. Anti-recommendation: adopt no framework. 22 sources.
- [x] [10-quality-metrics-scorecards.md](data-quality/10-quality-metrics-scorecards.md) — Why a composite quality score is ceremony for a solo operator and a **queryable history of individual check results** is the load-bearing artifact. Triton's `integrity_checks` audited as the proof: 776 rows / 95 run days / 8 checks, **zero `fail` ever** (structurally — no check function can return it), `could-not-evaluate` mapped to `pass` in three places (`:108`, `:167`, `:284` — an ingest outage turns the matview check green), the history silently dropping rows when the `cron_runs` lookup misses, and `found` censored at `LIMIT 200` so it can't be trended. The three-state severity model (`pass`/`breached`/`could-not-evaluate`); scorecard failure modes (dilution, weight theater, cross-asset blindness, slope invisibility); the vanity-dashboard test; Midas-style certification as a social contract. Ships the concrete `integrity_checks` extension (`observed_value`, `threshold`, `window_*`, `target_*`) and four queries — 4-week coverage slope, never-passed, flipped-this-week, MTBF per check. Anti-recommendation: build no score, buy no vendor. 23 sources.
- [x] [11-remediation-backfill-safety.md](data-quality/11-remediation-backfill-safety.md) — Repairing bad data without making it worse, built around the **2026-08-11 Stuff+ repair**. Why the backfill route that existed to fix this outage had **never once worked**: a `COUNT(*) ... LIMIT 1 OFFSET n` `hasMore` probe that is always empty for n > 0, and **`ctid` paging over an UPDATE, which is self-corrupting rather than merely unstable** because batch 1 relocates its own rows. The guard predicate (`stuff_plus IS NULL`) as safety *and* performance — 657k rows → ~250k, idempotent, April/May left untouched. Chunking by an indexed, **immutable** column with half-open ranges; resumability, cursors, and the **wall-clock budget the rewrite still lacks** (~180 chunks × ~2s ≈ 360s vs Vercel's 300s ceiling, no retry). Dry-run counts that must reproduce the UPDATE's `FROM` clause exactly; blast-radius table (~7.25M index entries). **Two-half verification** — coverage 0%→99.6% *and* league-average Stuff+ holding ~100.2–101.0, the invariant that would catch a skewed repair. Bloat: 1,437,923 dead tuples at 80.9% of the autovacuum trigger, and why the repo's VACUUM convention is **unimplementable** through any RPC. Anti-recommendation: no generic backfill framework, and `rescore` is not the default. 19 sources.
